#include <stdio.h>

void main()  
{  
    char myCharacter; // Variable declaration
    printf("\nEnter a characater: ");

    scanf("%c", &myCharacter); // User input

    printf("The caracter's decimal value acording ASCII table is: %d", myCharacter); // Use %d to print decimal values
    printf("\n ");
}  